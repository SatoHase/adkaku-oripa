---
// ステマ規制対応のPR表記。文言を1か所に集約するためのコンポーネント。
// 既定では Base.astro が本文冒頭に出す。位置を変えたいページは Base に prNote={false} を渡し、
// 任意の場所でこのコンポーネントを置く（置き忘れを防ぐため既定は「出す」側にしてある）。
---

<p class="prnote">※当ページにはアフィリエイト広告（PR）が含まれます。</p>
